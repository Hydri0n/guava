spring:
    datasource:
        url: jdbc:mysql://localhost:3306/guava
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
mybatis:
    # mapper.xml映射路径
    mapper-locations: classpath:mapper/*.xml
    # 数据库表映射实体所在包路径
    type-aliases-package: xyz.hplus.guava.entity
